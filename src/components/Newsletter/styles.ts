import styled from "styled-components";
import { theme } from "../../styles/theme";

export const NewsletterContainer = styled.div`
  margin: 0 auto;
  margin-top: 2rem;
  display: flex;
  align-items: center;
  background: white;
  box-shadow: ${theme.shadows.lightGray};
  width: 585px;
`
